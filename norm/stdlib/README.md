# Standard library

This directory contains the standard-library API and implementations written in Norm. Java platform adapters belong to `tool/core`.

The standard library must not introduce Zig or expose host Java types through its public Norm API.
