package std.core

import std.annotation.Document

@Document(description: "Represents either a successful value of type T or an error value of type E.")
enum Result<T, E> { Ok(T value), Err(E error) }
