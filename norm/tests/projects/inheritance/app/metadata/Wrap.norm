package app.metadata

import std.annotation.FieldInterceptor
import std.annotation.RuntimeRetention

public annotation Wrap implements FieldInterceptor<String>, RuntimeRetention {
  public String before(FieldContext context, String value) {
    printLine(context.field().name())
    return "[" + value + "]"
  }
}
