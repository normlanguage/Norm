package app.metadata

import std.annotation.FieldTarget
import std.annotation.RuntimeRetention

public annotation Wrap implements FieldTarget<String>, RuntimeRetention {
  public String before(FieldContext context, String value) {
    printLine(context.name())
    return "[" + value + "]"
  }
}
