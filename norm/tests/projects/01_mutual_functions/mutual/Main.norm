package mutual

import std.io.printLines

import std.testing.expectedOutputLines

Void main() {
  printLines([
    even(0),
    odd(7),
    even(10)
  ])
  expectedOutputLines([
    "true",
    "true",
    "true"
  ])
}
