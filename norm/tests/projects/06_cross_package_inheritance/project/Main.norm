package project

import project.impl.Child
import project.model.Base

main() {
  Child child = Child(initial: "Norm", initialRank: 9)
  Base<String> base = child
  printLine(base.get())
  printLine(child.rank)
  expectedOutputLine("Norm")
  expectedOutputLine("9")
}
