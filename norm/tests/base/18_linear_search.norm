int find(Array values, int target) {
  for index : range(start: 0, end: values.length) {
    if values[index] == target { return index }
  }
  return -1
}
void main() { print(find(values: [5, 8, 13, 21], target: 21)) }
