Void main() {
  Map<String, Integer> scores = Map<String, Integer>()
  scores.put(key: "Ada", value: 2)
  printLine(scores["Ada"])
  printLine(scores.containsKey("Ada"))
  expectedOutputLine("2")
  expectedOutputLine("true")
}
