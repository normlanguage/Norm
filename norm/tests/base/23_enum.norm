import std.testing.expectedOutputLines

enum Color { Red, Green, Blue }
Color favorite() { return Color.Green }
Void main() {
  Color color = favorite()
  printLine(color)
  printLine(color == Color.Green)
  expectedOutputLines([
    "Color.Green",
    "true"
  ])
}
