enum Color { Red, Green, Blue }
Color favorite() { return Color.Green }
Void main() {
  Color color = favorite()
  printLine(color)
  printLine(color == Color.Green)
  expectedOutputLine("Color.Green")
  expectedOutputLine("true")
}
