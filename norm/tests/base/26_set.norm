import std.io.printLines
import std.testing.expectedOutputLines

main() {
  Set<Integer> values = Set<>()
  values.add(5)
  values.add(5)
  values.add(8)
  printLines([values.contains(8), values.size()])
  expectedOutputLines(["true", "2"])
}
