import std.io.printLines
import std.testing.expectedOutputLines

class Box<T> {}

Box<T> emptyBox<T>() {
  return Box<>()
}

main() {
  Box<String> box = emptyBox()
  Array<Box<String>> values = []
  printLines([values.size(), box == box])
  expectedOutputLines(["0", "true"])
}
