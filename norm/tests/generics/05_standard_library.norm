import std.io.printLines
import std.testing.expectedOutputLines

main() {
  List<String> empty = []
  List<String> values = ["first", "last"]
  List<String> result = values.reversed()
  printLines([empty.size(), result[0], result[1]])
  expectedOutputLines(["0", "last", "first"])
}
