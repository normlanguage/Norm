class Parity {
  Boolean even(Integer value) {
    if value == 0 { return true }
    return this.odd(value - 1)
  }

  private Boolean odd(Integer value) {
    if value == 0 { return false }
    return this.even(value - 1)
  }
}

Void main() {
  Parity parity = Parity()
  printLine(parity.even(0))
  printLine(parity.even(7))
  printLine(parity.even(10))
  expectedOutputLine("true")
  expectedOutputLine("false")
  expectedOutputLine("true")
}
