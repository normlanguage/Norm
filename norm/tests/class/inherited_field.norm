import std.testing.expectedOutputLines

class Base {
  String name

  Base(String initial) {
    name = initial
  }
}

class Child extends Base {
  Child(String initial) {
    super(initial: initial)
  }
}

main() {
  printLine(Child(initial: "child").name)
  expectedOutputLines(["child"])
}

