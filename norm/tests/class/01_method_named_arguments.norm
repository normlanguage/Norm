class Point {
  Integer x
  Integer y

  Void move(Integer dx, Integer dy) {
    x = x + dx
    y = y + dy
  }
}

Void main() {
  Point point = Point(x: 1, y: 2)
  point.move(dy: 4, dx: 3)
  printLine(point.x)
  printLine(point.y)
  expectedOutputLine("4")
  expectedOutputLine("6")
}
