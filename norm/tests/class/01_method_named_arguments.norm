class Point {
  int x
  int y

  void move(int dx, int dy) {
    x = x + dx
    y = y + dy
  }
}

void main() {
  Point point = Point(x: 1, y: 2)
  point.move(dy: 4, dx: 3)
  print(point.x)
  print(point.y)
}
