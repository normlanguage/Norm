import std.io.printLines
import std.testing.expectedOutputLines

main() {
  List<String?> values = List.filled(size: 2, value: null)
  values[1] = "ready"
  printLines([values[0] ?? "missing", values[1] ?? "missing"])
  expectedOutputLines(["missing", "ready"])
}
