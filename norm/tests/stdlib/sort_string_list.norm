import std.collections.sort
import std.testing.expectedOutputLines

main() {
  List<String> source = List<>()
  source.add("😀")
  source.add("AA")
  source.add("A")
  source.add("é")
  for value : sort(values: source) {
    printLine(value)
  }
  expectedOutputLines(["A", "AA", "é", "😀"])
}
