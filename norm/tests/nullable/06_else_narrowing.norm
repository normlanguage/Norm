import std.testing.expectedOutputLines

Void show(String? value) {
  if value == null {
    printLine("none")
  } else {
    printLine(value)
  }
}

Void main() {
  show(value: null)
  show(value: "ready")
  expectedOutputLines([
    "none",
    "ready"
  ])
}
