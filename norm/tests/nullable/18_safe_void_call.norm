import std.testing.expectedOutputLines

class Sink {
  Void write() {
    printLine("called")
  }
}

main() {
  Sink? missing = null
  Sink? present = Sink()
  missing?.write()
  present?.write()
  printLine("done")
  expectedOutputLines(["called", "done"])
}
