import std.testing.expectedOutputLines

class Sink {
  Void write() { printLine("called") }
}

Void main() {
  Sink? missing = null
  Sink? present = Sink()
  missing?.write()
  present?.write()
  printLine("done")
  expectedOutputLines([
    "called",
    "done"
  ])
}
