import std.io.printLines
import std.testing.expectedOutputLines

class User {
  String name
}

main() {
  User? missing = null
  User? present = User(name: "Ada")
  printLines([missing?.name ?? "guest", present?.name ?? "guest"])
  expectedOutputLines(["guest", "Ada"])
}
