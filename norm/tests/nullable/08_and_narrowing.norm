Boolean longEnough(String? value) {
  return value != null && value.codePointSize() >= 3
}

Void main() {
  printLine(longEnough(value: null))
  printLine(longEnough(value: "abc"))
  expectedOutputLine("false")
  expectedOutputLine("true")
}
