import std.testing.expectedOutputLines

String fallback() {
  printLine("fallback")
  return "default"
}

main() {
  String? present = "value"
  String? missing = null
  printLine(present ?? fallback())
  printLine(missing ?? fallback())
  expectedOutputLines(["value", "fallback", "default"])
}
