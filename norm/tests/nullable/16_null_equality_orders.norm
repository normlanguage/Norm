import std.io.printLines
import std.testing.expectedOutputLines

main() {
  String? value = null
  printLines([value == null, null == value, value != null, null != value])
  expectedOutputLines(["true", "true", "false", "false"])
}
