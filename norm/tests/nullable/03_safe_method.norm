class Word {
  String value
  Integer size() { return value.codePointSize() }
}

Void main() {
  Word? missing = null
  Word? present = Word(value: "你好")
  printLine(missing?.size() ?? 0)
  printLine(present?.size() ?? 0)
  expectedOutputLine("0")
  expectedOutputLine("2")
}
