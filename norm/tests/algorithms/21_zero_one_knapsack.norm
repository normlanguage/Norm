int knapsack(Array weights, Array values, int capacity) {
  List best = List()
  for value : range(start: 0, end: capacity + 1) { best.add(0) }
  for item : range(start: 0, end: weights.length) {
    for step : range(start: 0, end: capacity - weights[item] + 1) {
      int current = capacity - step
      best[current] = max(left: best[current], right: best[current - weights[item]] + values[item])
    }
  }
  return best[capacity]
}

void main() {
  print(knapsack(weights: [2, 3, 4], values: [4, 5, 7], capacity: 5))
}
