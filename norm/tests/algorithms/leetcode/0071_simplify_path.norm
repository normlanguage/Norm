String simplifyPath(String path) {
  Array<String> components = path.split(separator: "/")
  List<String> stack = List<>()
  for String component : components {
    if component.isEmpty() || component == "." {} else if component == ".." {
      if !stack.isEmpty() {
        stack.removeLast()
      }
    } else {
      stack.add(component)
    }
  }
  StringBuilder result = StringBuilder()
  for String component : stack {
    result.append("/")
    result.append(component)
  }
  if stack.isEmpty() {
    return "/"
  }
  return result.toString()
}

main() {
  printLine(simplifyPath("/home/user/../docs/"))
  expectedOutputLine("/home/docs")
}
