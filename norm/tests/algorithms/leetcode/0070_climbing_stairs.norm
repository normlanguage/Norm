Integer climbStairs(Integer steps) {
  Integer previous = 0
  Integer current = 1
  for step : range(start: 0, end: steps) {
    Integer next = previous + current
    previous = current
    current = next
  }
  return current
}

main() {
  printLine(climbStairs(5))
  expectedOutputLine("8")
}
