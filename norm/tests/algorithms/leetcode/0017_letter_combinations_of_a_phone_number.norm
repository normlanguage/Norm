String append(String prefix, CodePoint value) {
  StringBuilder result = StringBuilder()
  result.append(prefix)
  result.append(value)
  return result.toString()
}

List<String> letterCombinations(String digitsValue) {
  Array<CodePoint> digits = digitsValue.codePoints()
  Array<String> letters = ["","","abc","def","ghi","jkl","mno","pqrs","tuv","wxyz"]
  List<String> combinations = List<String>()
  combinations.add("")
  for CodePoint digit : digits {
    String choices = letters[digit.scalarValue() - '0'.scalarValue()]
    Array<CodePoint> points = choices.codePoints()
    List<String> next = List<String>()
    for String prefix : combinations {
      for CodePoint point : points { next.add(append(prefix: prefix, value: point)) }
    }
    combinations = next
  }
  return combinations
}

Void main() {
  List<String> answer = letterCombinations("23")
  printLine(answer.size())
  printLine(answer[0])
  expectedOutputLine("9")
  expectedOutputLine("ad")
}
