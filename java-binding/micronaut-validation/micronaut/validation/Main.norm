package micronaut.validation

import micronaut.validation.Validated

@Validated()
class RegistrationService {
  String register(String username) {
    return username
  }
}

Void main() {
  printLine(RegistrationService().register(username: "Norm"))
}
