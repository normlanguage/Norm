# Norm Logging Library

Logging is a standard application capability.

Features:
- levels
- structured fields
- JSON output
- tracing integration

Example:
```norm
Logger.info(message = "User created", userId = id)
```

