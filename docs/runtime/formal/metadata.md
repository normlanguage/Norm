# Norm Runtime Metadata

Runtime metadata supports:

- reflection
- annotations
- generic types
- dynamic dispatch

Metadata is retained explicitly rather than inferred from erased runtime structures.
