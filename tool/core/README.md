# Core

The Java implementation of the Norm compiler, runtime, and GraalVM/Truffle execution engine. Shared immutable data lives in `dev.w0fv1.norm.value`; stateless cross-project tools live in `dev.w0fv1.norm.utils`.
